#ifndef CONFIG_H
#define CONFIG_H

#include <cmath>

// ============================================================
// 1. تنظیمات ابعاد و جمعیت (Simulation Sizing)
// ============================================================
#define DIM 100           // تعداد ابعاد پنگوئن
#define POP_SIZE 20      // تعداد کل جمعیت
#define MAX_ITER 100     // تعداد کل تکرارهای شبیه‌سازی

// ============================================================
// 2. تنظیمات بازه جستجو (Search Space Boundaries)
// ============================================================
const double LB = -10.0; // کران پایین (Lower Bound)
const double UB =  10.0; // کران بالا (Upper Bound)

// ============================================================
// 3. تنظیمات استراتژی همسایگی (Topology Strategy)
// ============================================================
// 0: ALL_PAIRS (دقیق، سنگین - O(D^2))
// 1: RANDOM    (سریع، تقریبی - O(D*sqrt(D)))
#define STRATEGY_ID 1 

// تعداد همسایگان در حالت رندوم (معمولاً جذر ابعاد)
// اگر استراتژی 0 باشد، این عدد نادیده گرفته می‌شود.
const int N_NEIGHBORS = (int)std::sqrt(DIM); 

// ============================================================
// 4. تنظیمات هدف بهینه‌سازی (Optimization Goal)
// ============================================================
// 0: MINIMIZE (کمترین مقدار بهتر است - مثل Cost Function)
// 1: MAXIMIZE (بیشترین مقدار بهتر است - مثل Fitness Function)
#define OPT_MODE 0 

// ============================================================
// 5. نوع تابع هدف (Benchmark Function)
// ============================================================
// 0: ROSENBROCK
// 1: SPHERE
#define FUNC_ID 1

// ============================================================
// 6. پارامترهای الگوریتم EPC (Algorithm Hyperparameters)
// ============================================================
const double M_INIT = 0.5;   // مقدار اولیه جهش (Mutation)
const double MU_INIT = 0.05; // مقدار اولیه گرما (Heat)
const double COOLING_RATE = 0.99; // نرخ کاهش دما

#endif // CONFIG_H