#ifndef CONFIG_H
#define CONFIG_H

#include <cmath>

// ============================================================
// 1. تنظیمات ابعاد و جمعیت (Simulation Sizing)
// ============================================================
#define DIM 100           // تعداد ابعاد مسئله (تعداد متغیرهایی که باید بهینه شوند)
#define POP_SIZE 20       // تعداد کل جمعیت پنگوئن‌ها (تعداد واحدهای پردازشی موازی)
#define MAX_ITER 100      // تعداد کل دورهای شبیه‌سازی (تکرار حلقه اصلی)

// ============================================================
// 2. تنظیمات بازه جستجو (Search Space Boundaries)
// ============================================================
const double LB = -10.0;  // کران پایین فضای جستجو (حداقل مقدار مجاز برای متغیرها)
const double UB =  10.0;  // کران بالای فضای جستجو (حداکثر مقدار مجاز برای متغیرها)

// ============================================================
// 3. تنظیمات استراتژی همسایگی (Topology Strategy)
// ============================================================
// 0: ALL_PAIRS (همه با هم ارتباط دارند - دقیق‌تر ولی کندتر)
// 1: RANDOM    (هر پنگوئن فقط با تعدادی تصادفی ارتباط دارد - سریع‌تر)
#define STRATEGY_ID 1

// تعداد همسایگان در حالت رندوم (معمولاً جذر تعداد ابعاد در نظر گرفته می‌شود)
const int N_NEIGHBORS = (int)std::sqrt(DIM); 

// ============================================================
// 4. تنظیمات هدف بهینه‌سازی (Optimization Goal)
// ============================================================
// 0: MINIMIZE (هدف: پیدا کردن کمترین مقدار - مثل تابع هزینه)
// 1: MAXIMIZE (هدف: پیدا کردن بیشترین مقدار - مثل تابع برازش)
#define OPT_MODE 0

// ============================================================
// 5. نوع تابع هدف (Benchmark Function)
// ============================================================
// 0: ROSENBROCK (یک تابع پیچیده و سخت برای تست)
// 1: SPHERE (یک تابع ساده؛ مجموع توان دوم‌ها)
#define FUNC_ID 0

// ============================================================
// 6. پارامترهای فیزیکی الگوریتم EPC (Algorithm Hyperparameters)
// ============================================================
const double M_INIT = 0.05;        // مقدار اولیه برای دامنه نویز یا جهش (Movement/Mutation)
const double MU_INIT = 0.05;      // مقدار اولیه ضریب جذب (شعاع تاثیرگذاری پنگوئن‌ها روی هم)
const double A_INIT = 0.05;
const double COOLING_RATE = 0.99; // نرخ کاهش دما (در هر دور، M و MU در این عدد ضرب می‌شوند تا دقیق‌تر شوند)

#endif // CONFIG_H